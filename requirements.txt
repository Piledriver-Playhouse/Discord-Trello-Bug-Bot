# =============================================================================
# requirements.txt — Runtime Python dependencies
# =============================================================================
#
# Install with:
#   pip install -r requirements.txt
#
# Version pins use compatible ranges to allow patch updates while
# preventing unexpected major-version breaking changes.
# =============================================================================

# Discord API wrapper (gateway connection, command parsing, intents).
discord.py>=2.3,<3

# Async HTTP client used to call the Trello REST API.
aiohttp>=3.9,<4

# Loads .env files into os.environ for local development convenience.
# In Docker/k8s, env vars are injected by the runtime, so this is a no-op.
python-dotenv>=1.0,<2

# Web templating engine for the dashboard.
jinja2>=3.1,<4
aiohttp_jinja2>=1.5,<2
